# Vertex Finding Model

UNet (PV-Finder) that maps track features to a 1-D z-axis histogram (12,000 bins, 0.04 mm each).

Input: 7-channel track features per sub-event.
Output: continuous histogram over z-axis, peaks indicate vertex positions.

(to be expanded as model code is migrated)

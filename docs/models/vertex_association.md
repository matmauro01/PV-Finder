# Vertex Association Model

Graph Attention Network (GNN) that post-filters PVF peaks by scoring track-vertex associations.

Input: graph built from PVF output — tracks as nodes, candidate vertices as targets.
Output: per-peak quality score used to reject fakes.

(to be expanded as model code is migrated)
